#include<stdio.h>
#include<stdlib.h>
int main()
{
int N, result[32], index=0;
scanf("%d",&N);
int B;
scanf("%d",&B);
while(N>0)
{
    result[index]=N%B;
    N=N/B;
    index++;
}
for(int i=index-1; i>=0; i--)
{
    printf("%d", result[i]);
}
}
